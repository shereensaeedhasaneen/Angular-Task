import { ChangeBackgroundOnHoverDirective } from './change-background-on-hover.directive';

describe('ChangeBackgroundOnHoverDirective', () => {
  it('should create an instance', () => {
    const directive = new ChangeBackgroundOnHoverDirective();
    expect(directive).toBeTruthy();
  });
});
